<% if !@home_articles.empty? %>
    <h4>Recent Articles</h4>
    <ul class="list-group">
      <% @home_articles.each do |article| %>
          <li class="my-list-item"><%= link_to_unless_current article.title, article %></li>
      <% end %>
    </ul>
<% end %>